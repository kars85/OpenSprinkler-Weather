// Global describe/it/expect/beforeEach/afterEach types (vitest supplies these at runtime via
// `globals: true`). Replaces @types/mocha, which was removed with the mocha runner.
/// <reference types="vitest/globals" />
